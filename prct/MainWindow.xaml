<Window x:Class="prct.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Topics Viewer" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="173*"/>
            <RowDefinition Height="34*"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="TopicsDataGrid"
                  ItemsSource="{Binding Topics}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="300"/>
                <DataGridTextColumn Header="Дата создания" Binding="{Binding CreatedAt, StringFormat=dd.MM.yyyy}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="1" HorizontalAlignment="Left">
            <Button x:Name="AddTopicAsync"
                    Content="Добавить"
                    Height="20" 
                    Width="100"
                    Margin="0,5,0,5" Click="AddTopic_Click"/>
            <Button x:Name="DeleteTopicAsync"
                    Content="Удалить" 
                    Height="20" 
                    Width="100" Click="DeleteTopic_Click"/>
        </StackPanel>
    </Grid>
</Window>
